from typing import Any

import numpy as np
import torch

# Type alias for data dictionaries
DataDict = dict[str, Any]

# Type alias for array types (numpy or torch)
ArrayType = np.ndarray | torch.Tensor
